fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-multipart==0.0.20

# Database (PostgreSQL)
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async driver, used by the app at runtime
# psycopg2-binary==2.9.10  # sync driver, used by Alembic for migrations

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# CORS is built into fastapi.middleware.cors, no extra package needed

# Testing
pytest==8.3.4
httpx==0.28.1

pydantic-settings>=2.0